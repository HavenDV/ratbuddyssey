<views:FileViewBase
    x:Class="Ratbuddyssey.Views.FileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rx="using:ReactiveUI"
    xmlns:views="using:Ratbuddyssey.Views"
    xmlns:viewModels="using:Ratbuddyssey.ViewModels"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:e="using:H.XamlExtensions"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d"
    AllowDrop="True"
    >
    <!--xmlns:mtuuc="using:Microsoft.Toolkit.Uwp.UI.Controls"-->
    <Grid>
        <Grid e:GridExtensions.ColumnsAndRows="A,A,*;A,*">
            <muxc:MenuBar Grid.Row="0" Grid.Column="0">
                <muxc:MenuBarItem Title="Commands">
                    <MenuFlyoutItem Text="Open Audyssey file" x:Name="OpenFileMenuItem"/>
                    <MenuFlyoutItem Text="Reload (discard unsaved changes)" x:Name="ReloadFileMenuItem"/>
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="Save" x:Name="SaveFileMenuItem"/>
                    <MenuFlyoutItem Text="Save As" x:Name="SaveFileAsMenuItem"/>
                </muxc:MenuBarItem>
            </muxc:MenuBar>

            <Grid
                Grid.Row="1"
                Grid.Column="0"
                e:GridExtensions.Rows="*,A,A,A,A"
                >
                <!--<views:ChannelsView
                    Grid.Row="0"
                    x:Name="ChannelsView"
                    d:DataContext="{d:DesignInstance
                        viewModels:DesignTimeChannelsViewModel, 
                        IsDesignTimeCreatable=True}"
                    />-->
                <!--<mtuuc:GridSplitter 
                    Grid.Row="1" 
                    HorizontalAlignment="Stretch" 
                    Height="5" 
                    />-->
                <!--<views:ChannelInformationView
                    Grid.Row="2"
                    x:Name="ChannelInformationView"
                    />-->
                <!--<mtuuc:GridSplitter
                    Grid.Row="3"
                    Height="5"
                    HorizontalAlignment="Stretch"
                    />-->
                <!--<views:ChannelReportView
                    Grid.Row="4"
                    x:Name="ChannelReportView"
                    />-->
            </Grid>
            <!--<mtuuc:GridSplitter 
                Grid.Row="0" 
                Grid.RowSpan="2" 
                Grid.Column="1" 
                Width="5" 
                HorizontalAlignment="Stretch"
                />-->
            <Grid 
                Grid.Row="0" 
                Grid.RowSpan="2" 
                Grid.Column="2"
                e:GridExtensions.Rows="A,A,*"
                >
                <views:StatusView
                    Grid.Row="0"
                    x:Name="StatusView"
                    />
                <!--<mtuuc:GridSplitter 
                    Grid.Row="1" 
                    Height="5" 
                    HorizontalAlignment="Stretch" 
                    />-->
                <Grid
                    Grid.Row="2"
                    e:GridExtensions.Columns="A,A,*"
                    >
                    <!--<views:TargetCurvePointsView 
                        Grid.Column="0"
                        x:Name="TargetCurvePointsView"
                        d:DataContext="{d:DesignInstance
                            viewModels:DesignTimeTargetCurvePointsViewModel, 
                            IsDesignTimeCreatable=True}"
                        />-->
                    <!--<mtuuc:GridSplitter
                        Grid.Column="1"
                        Width="5"
                        HorizontalAlignment="Stretch"
                        />-->
                    <!--<views:GraphView
                        Grid.Column="2"
                        x:Name="GraphView"
                        d:DataContext="{d:DesignInstance
                            viewModels:DesignTimeGraphViewModel, 
                            IsDesignTimeCreatable=True}"
                        />-->
                </Grid>
            </Grid>
        </Grid>

        <views:PreviewDropView
            x:Name="PreviewDropView"
            d:Visibility="Collapsed"
            />
    </Grid>
</views:FileViewBase>
